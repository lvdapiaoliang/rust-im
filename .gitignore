# 构建产物（每次 cargo build 重新生成，体积巨大，绝不入库）
/target

# SDK 打包产物（cargo xtask sdk 随时可重建，不入库）
/dist

# 文档站构建产物（mdbook build 随时可重建，不入库；源在 docs/ + book.toml）
/book


# RustRover / IntelliJ 局部文件
.idea/workspace.xml
.idea/shelf/

# Windows
Thumbs.db
