[package]
name = "im-transport"
description = "传输层：Tokio TCP 长连接、心跳、断线重连、seq/ack、TLS(rustls)、后期 QUIC(quinn)"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true

[lints]
workspace = true

[dependencies]
bytes.workspace = true
im-protocol.workspace = true
thiserror.workspace = true
tokio.workspace = true
# 阶段 12 TLS：材料（证书/配置）来自 im-crypto，装配（TcpStream → TLS 流 → 帧流）在这里
im-crypto.workspace = true
rustls.workspace = true
tokio-rustls.workspace = true
# 阶段 13 QUIC：quinn 只在这里出现——装配层把 quinn 类型全部包住，
# 上游（im-server/demo）只看到 rustls 配置 + QuicAcceptor/QuicConnector
quinn.workspace = true

[dev-dependencies]
anyhow.workspace = true
